{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Generation Details Metadata",
  "version": "1.0",
  "type": "object",
  "properties": {
    "inputs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of inputs used during generation (e.g., files, data, hashes)"
    },
    "parameters": {
      "type": "object",
      "description": "Key-value pairs of parameters used during generation"
    },
    "toolVersion": {
      "type": "string",
      "description": "Version of the tool or process used"
    },
    "steps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional: Steps followed during the generation process"
    },
    "environment": {
      "type": "string",
      "description": "Optional: Environment in which the generation took place"
    },
    "remarks": {
      "type": "string",
      "description": "Optional: Any additional remarks or notes"
    }
  },
  "required": ["inputs", "toolVersion"]
}
